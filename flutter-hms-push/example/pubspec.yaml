name: huawei_push_example
description: Demonstrates how to use the huawei_push plugin.

environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  huawei_push:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
    
flutter:
  uses-material-design: true
